{
  "name": "pgsql-grammar",
  "version": "18.0.0",
  "private": true,
  "description": "Standalone PostgreSQL TextMate grammar",
  "license": "MIT",
  "scripts": {
    "test": "node --test scripts/test.mjs",
    "validate": "node scripts/validate.mjs",
    "validate-sql": "node scripts/validate-sql.mjs",
    "preview": "node scripts/preview.mjs"
  },
  "dependencies": {
    "shiki": "^3.22.0"
  },
  "devDependencies": {
    "libpg-query": "^17.7.3"
  }
}
